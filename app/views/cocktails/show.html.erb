<div class="container">
  <div class="card">
    <h1><%= @cocktail.name %></h1>
  </div>
  <h2>Add Ingredients</h2>
  start form to add ingredients
</div>

<% @cocktail.photos.each do |photo| %>
  <%= cl_image_tag photo.path, width: 300, height: 200, crop: :fill %>
<% end %>
